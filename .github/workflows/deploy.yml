name: Deploy to Skynet
on:
  pull_request:
jobs:
  build:
  runs-on: ubuntu-latest
steps:
  — uses: actions/checkout@v2
  - uses: actions/setup-node@v2-beta
      with:
        node-version: "14"
  - name: Install dependencies
    run: npm install
  - name: Build
    run: npm run build
  - name: "Deploy to Skynet"
    uses: rajatbeladiya/SkyNetGram@master
    with:
    upload-dir: build
    github-token: ${{ secrets.GITHUB_TOKEN }}
